{
    "name": "my-app",
    "version": "1.0.0",
    "scripts": {
        "start": "npm-run-all --parallel backend:dev frontend:dev",
        "backend:dev": "cd backend && npm run dev",
        "frontend:dev": "cd frontend && npm run dev"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}
